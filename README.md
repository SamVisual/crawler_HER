# crawler_HER

A specialized crawler created with GPT.

## Setup

Install dependencies:

```bash
pip install -r requirements.txt
```

## Usage

Run the validator with an Excel file:

```bash
python validate.py --file customers.xlsx --delay 2-5
```

The script creates a file `customers_validated.xlsx` with validation results.
